<Page x:Class="BreweryApp.Views.DirectorPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Панель директора"
                   FontSize="22"
                   FontWeight="Bold"
                   Margin="0,0,0,10"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Отчеты по заказам" Click="OrdersReport_Click"/>
            <Button Content="Отчеты по складу" Click="StockReport_Click"/>
            <Button Content="Отчеты по задачам" Click="TasksReport_Click"/>
        </StackPanel>

        <DataGrid Name="DirectorGrid"
                  Grid.Row="2"
                  AutoGenerateColumns="True"
                  IsReadOnly="True"/>
    </Grid>
</Page>